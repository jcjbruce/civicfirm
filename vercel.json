{
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/proposals/(.*)", "destination": "/proposals/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
